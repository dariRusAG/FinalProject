{% extends 'base.html.twig' %}

{% block title %}Книги{% endblock %}

{% block body %}
    <h1>Книги</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ book.id }}</td>
            </tr>
            <tr>
                <th>Жанр</th>
                <td>{{ book.genreBook }}</td>
            </tr>
            <tr>
                <th>Название</th>
                <td>{{ book.nameBook }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_books_index') }}">назад</a>

    <a href="{{ path('app_books_edit', {'id': book.id}) }}">редактировать</a>

    {{ include('books/_delete_form.html.twig') }}
{% endblock %}
